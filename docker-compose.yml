version: '3'

services:
  newapi-status:
    build: .
    container_name: newapi-status
    restart: always
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    # 如果没有 .env 文件，可以使用以下环境变量覆盖
    # environment:
    #   - NEWAPI_URL=https://api.xinjianya.top/api/data/
    #   - NEWAPI_KEY=your_key_here
    #   - NEWAPI_ADMIN_ID=1
    #   - SITE_TITLE=星见雅API
